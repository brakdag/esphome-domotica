esphome:
   name: esp01-relay 
   
esp8266:
  board: esp01_1m

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

api:

ota:
  platform: esphome 

logger:

switch:
  - platform: gpio
    pin: GPIO0
    name: termotaque